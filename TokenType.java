enum TokenType {
    NUMBER,          // 123, 3.14
    OPERATOR,        // + - * /
    CELL_REF,        // A1, AA12
    FUNCTION,        // SUM, MIN, MAX, MEAN
    LEFT_PAREN,      // (
    RIGHT_PAREN,     // )
    ARG_SEPARATOR,   // ;
    RANGE_SEPARATOR,
    ARG_MARKER

}

